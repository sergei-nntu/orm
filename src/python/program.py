from orm_http_server import *
def program_main(should_terminate_function, set_active_block_id):
  while True:
    set_active_block_id('^n5rC+a0~G+Lv;.tng:/')
    orm_blockly_set_position(0.1, 0.2, 0.4, 0, 0, 0)
    if should_terminate_function(): return
    set_active_block_id('SNtGh9XY[BhM_q$A4-/V')
    orm_blockly_delay(3000)
    if should_terminate_function(): return
    set_active_block_id('3{[=.$!xK!zS~F$NI0Mu')
    orm_blockly_set_position(0, 0.2, 0.4, 0, 0, 0)
    if should_terminate_function(): return
    set_active_block_id('rf;:%_UP3$DIE5IzE^}S')
    orm_blockly_delay(3000)
    if should_terminate_function(): return
